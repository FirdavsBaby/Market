import { useEffect } from "react";

function allProducts(state = [] , action) {
    switch (action.type) {
        case "ALL_PRODUCTS": {
         let Products = {
            ...state,
            
         }
        }
            
            
    
        default: 
    }    
}

export default allProducts